rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write if user is authenticated.
      // In production, tighten this to specific paths (e.g., users/{uid}/avatar.png)
      allow read, write: if request.auth != null;
    }
  }
}